# ── Core pipeline ───────────────────────────────────────────
pandas>=2.0
numpy>=1.25
openpyxl>=3.1          # Excel read/write
pyyaml>=6.0            # config.yaml parsing
schedule>=1.2          # job scheduling
pyarrow>=14.0          # Parquet support

# ── Google Drive ─────────────────────────────────────────────
google-auth>=2.20
google-auth-oauthlib>=1.0
google-auth-httplib2>=0.1
google-api-python-client>=2.90

# ── Dashboard ────────────────────────────────────────────────
streamlit>=1.32
plotly>=5.18

# ── Phase 2: ODBC (uncomment when Magic Touch confirms) ──────
# pyodbc>=5.0
