"""Removable — Employee Settings (pay, task rates, goals). See settings_page.py."""
from settings_page import render_settings

render_settings(dashboard="Removable", label="Removable", pw_key="removable")
